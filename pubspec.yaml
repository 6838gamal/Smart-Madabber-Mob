name: smart_maddaber
description: Smart Advisor / المدبّر الذكي — Intelligent Decision-Making System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  fl_chart: ^0.69.0
  intl: ^0.20.2
  uuid: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
